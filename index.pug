---
heading: Markdown to Javascript
subtitle: Pipedown turns your markdown into executable pipelines
heroImage: /img/hero.png
---

include _includes/hero.pug
include _includes/howToInstall.pug


.bg-white.mx-auto.max-w-7xl.px-6(class="lg:px-0 lg:px-8 space-y-8 sm:space-y-10 md:space-y-12 mb-24 sm:md-28 md:mb-36")
  h2.text-3xl.font-bold.tracking-tight.text-gray-900(class="sm:text-4xl md:text-5xl")= title
    ul.mt-3.grid.grid-cols-1.gap-5(role="list" class="sm:grid-cols-2 sm:gap-6 lg:grid-cols-4")
      each val in cards
        li.col-span-1.flex.rounded-md.shadow-sm
          .flex.w-16.flex-shrink-0.items-center.justify-center.bg-pink-600.rounded-l-md.text-sm.font-medium.text-white !{val.icon}
          .flex.flex-1.items-center.justify-between.rounded-r-md.border-b.border-r.border-t.border-gray-200.bg-white
            .flex-1.px-4.py-2.text-sm
              a.font-medium.text-gray-900(href='#', class='hover:text-gray-600')= val.title
              p.text-gray-500= val.body

.bg-white.mx-auto.max-w-7xl.px-6(class="lg:px-0 lg:px-8 space-y-8 sm:space-y-10 md:space-y-12 mb-24 sm:md-28 md:mb-36")
  .prose.prose-2xl.mx-auto
    :md
      ## --help

      Pipedown is a markdown preprocessor that turns your markdown codeblocks into executable scripts.

      ```sh
      Pipedown
      Usage: pd [command] [options]

      Commands:
        build   Generate scripts for all markdown files in current directory
        run     Run a script generated by pd
        test    Run tests for all scripts generated by pd
        list    List all scripts generated by pd
        clean   Remove all scripts generated by pd
        help    You're reading it!

      Options:
        -d, --debug   Display debug information.
        -h, --help    Display this message.
        -j, --json    Output plain JSON.
        -p, --pretty  Pretty print JSON output (aka console.log!)
      ```

      ## Build a project

      Pipedown generates scripts for all markdown files in the current directory. 

      <div class="flex justify-around items-center">

      `pd build`

      ```sh
      # pd build generates this a directory like 👇
      .pd
        - my-pipe
          - index.ts
          - index.md
          - index.json
      ```

      </div>

      ## Run a script

      Run any codeblocks in your markdown files with the `pd run` command.

      ```sh
      pd run helloWorld.md 
      Markdown files processed: 1
      Hello World!
      ```

      ## Hello World! 
      Copy and paste the following markdown into a file called `helloWorld.md` and run `pd run helloWorld.md` to see the magic happen.
      ````md
      # Hello there
      
      ## Hello
      ```ts
      input.hello = 'Hello '
      ```

      ## World
      ```ts
      input.world = 'World!'
      ```

      ## togetherAgain
      ```ts
      console.log(input.hello + input.world)
      ```
      ````
.bg-white.mx-auto.max-w-7xl.px-6(class="lg:px-0 lg:px-8 space-y-8 sm:space-y-10 md:space-y-12 mb-24 sm:md-28 md:mb-36")
  .mx-auto
    h3.text-3xl.font-bold.tracking-tight.text-gray-900(class="sm:text-4xl md:text-5xl") Learn More!
    include _includes/heroNav.pug

script(src='/fetchJsrMeta/index.iife.js')
script(src='/toggleInstallTabs/index.iife.js')
script.
  PD.toggleInstallTabs.pipe.process()
  PD.fetchJsrMeta.pipe.process({ selector: '[name="pdcli-version"]' })